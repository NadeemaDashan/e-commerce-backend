spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invoice_generator
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true